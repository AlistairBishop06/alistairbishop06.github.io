# CV document

Place the CV PDF in this folder. Its filename can be anything: the Vite document resolver discovers it automatically and uses it for the on-screen CV, download, and print actions.

Keep one PDF here whenever possible. If several PDFs are present, the most recently modified one is selected. Adding, replacing, or renaming a PDF reloads the development site automatically; production deployments select the document during the build.
