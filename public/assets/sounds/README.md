# Sounds

The default sound set is synthesised with the browser Web Audio API in `src/hooks/useSound.ts`, so it is tiny, offline-capable, and does not redistribute proprietary Windows audio. Local `.wav`, `.mp3`, or `.ogg` replacements can be added here.
